name: demo-assistant
agent_id: demo-assistant
type: "openagents.agents.collaborator_agent.CollaboratorAgent"

config:
  model_name: "glm-4.5"
  provider: "openai"
  api_base: "https://open.bigmodel.cn/api/paas/v4"
  api_key: "f5d8c43c8f8c4b78a950606b5b178aac.8yr5KN2lhBPdeC6w"

  instruction: |
    你是一个友好的 Demo 助手。

    你的职责：
    - 回答用户问题
    - 展示多 Agent 系统的基本功能

    **重要**：收到消息后必须使用 send_direct_message 工具回复用户！

  triggers:
    - event: "thread.direct_message.notification"
      instruction: |
        用户发送了私信。使用 send_direct_message 工具回复用户。
        保持回复简洁友好。

    - event: "thread.channel_message.notification"
      instruction: |
        用户在频道发送了消息。使用 send_channel_message 工具回复。
        保持回复简洁友好。
