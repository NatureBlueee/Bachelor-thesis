# Academic Research Network - 项目分析与转型规划

> 版本: 1.0 | 日期: 2025-12-25
> 目标: 分析现有论文协作系统的独特性，规划如何转型为通用研究工具

---

## 第一部分：我们已经拥有的（现状梳理）

### 1.1 系统架构概览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                      论文工程化协作系统 (当前运行版本)                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  规则层 (.agent/rules/)              工作流层 (.agent/workflows/)           │
│  ├── thesis-project.md (AI身份)      ├── /start (启动检查)                  │
│  ├── consensus.md (学术问答)         ├── /create-pr (创建变更)               │
│  ├── writing.md (写作规范)           ├── /merge-pr (合并变更)                │
│  └── context-update.md               ├── /add-paper (添加文献)               │
│                                      ├── /ask-academic-ai (结构化提问)       │
│                                      ├── /analyze-answer (分析回答)          │
│                                      ├── /deep-read (深度阅读)               │
│                                      ├── /sync (状态同步)                    │
│                                      └── /reflect (记忆提取)                 │
│                                                                             │
│  数据层                                                                     │
│  ├── Reference/ (100+篇文献)         自动化工具                              │
│  │   ├── PDF-MD/convert_api.py      ├── PDF→Markdown (Datalab API)          │
│  │   ├── _INDEX.md                  ├── process.py (批量处理)               │
│  │   └── Cited/Uncited/             └── sync_citations.py (引用同步)        │
│  ├── Consensus/ (问答记录)                                                   │
│  ├── PR/ (9个PR)                                                            │
│  ├── Target/ (论文产出)                                                      │
│  ├── MEMORY.md (AI笔记)                                                      │
│  └── _STATE.md (状态快照)                                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 已实现的9个工作流

| 工作流 | 用途 | 独特价值 | 通用化难度 |
|--------|------|----------|-----------|
| `/start` | 新对话启动检查 | 5秒内让AI理解进度 | ⭐ 低 |
| `/create-pr` | 创建修改请求 | PR驱动变更管理 | ⭐ 低 |
| `/merge-pr` | 合并PR+强制引用检查 | 10步验证流程 | ⭐⭐ 中 |
| `/add-paper` | PDF→Markdown转换 | 自动重命名+索引更新 | ⭐ 低 |
| `/ask-academic-ai` | 四部分结构化提问 | 避免只得到结论 | ⭐ 低 |
| `/analyze-answer` | 分析回答+标记优先级 | 🔴🟡⚪分类体系 | ⭐ 低 |
| `/deep-read` | 批判性阅读文献 | 按命题关联度评价 | ⭐⭐ 中 |
| `/sync` | 状态同步 | 追加快照不覆盖 | ⭐ 低 |
| `/reflect` | 智能记忆提取 | 先评估"值不值得" | ⭐ 低 |

### 1.3 已实现的自动化工具

| 工具 | 文件 | 功能 | 通用化价值 |
|------|------|------|-----------|
| PDF转换 | `convert_api.py` | PDF→Markdown (Datalab API) | ⭐⭐⭐ 高 |
| 批量处理 | `process.py` | 重命名+索引更新 | ⭐⭐ 中 |
| 引用同步 | `sync_citations.py` | 检测Draft中缺失引用 | ⭐⭐⭐ 高 |

---

## 第二部分：我们的独特创新点 (vs OpenAgents Demo)

### 2.1 核心差异化

| 维度 | OpenAgents Demo (03_research_team) | 我们的系统 |
|------|-----------------------------------|-----------|
| **约束来源** | Prompt中写正面指令 | **负面约束优先** (❌禁止列表) |
| **变更管理** | 无结构化管理 | **PR驱动** (9个PR, 完整生命周期) |
| **记忆系统** | 无持久化 | **MEMORY.md + /reflect** 智能提取 |
| **状态同步** | 无 | **_STATE.md + /sync** 追加快照 |
| **工作流** | 隐式在Prompt中 | **9个独立工作流文件** |
| **文献处理** | 无 | **PDF→MD + 分类索引 + 引用检查** |
| **验证周期** | 真实使用时长 | **2个月+，9个PR，100+文献** |

### 2.2 独特创新点详解

#### 创新1：负面约束设计 (Negative Constraints)

```
传统做法：告诉AI"你应该做X"
我们的做法：告诉AI"你绝对不能做Y"

示例：
❌ 禁止直接编辑论文（必须通过PR）
❌ 禁止凭空捏造文献（必须来自Reference/）
❌ 禁止偏离核心（代际冲突等宏大话题）
❌ 禁止只给结论（必须提供颗粒度）
```

**价值**：AI幻觉的根源是"什么都敢做"。负面约束建立边界。

#### 创新2：PR驱动变更管理

```
文献变更：MEMORY.md记录 → 讨论 → 创建PR → 引用检查 → 合并 → 同步

PR生命周期：
planning → discussing → approved → merged

关键机制：
- 每个PR有模板
- 合并前强制10步Checklist
- 合并后必须同步_CONTEXT.md
```

**价值**：论文发展可追溯，决策有记录，不会"改着改着就乱了"。

#### 创新3：智能记忆系统

```
/reflect 工作流逻辑：

Step 0: 评估"值不值得记忆" → 如果不值得，直接返回"无需记忆"
Step 1: 回顾对话，用5个维度分析（偏好/决策/洞见/纠正/约束）
Step 2: 判断标准（会影响未来交互 = 值得记）
Step 3: 格式化 → 更新MEMORY.md
```

**价值**：记忆越用越精准，而不是越积越乱。

#### 创新4：状态追加快照

```
/sync 核心原则：追加快照，不是覆盖

_STATE.md 结构：
- 当前焦点（动态更新）
- 活跃PR（动态更新）
- 同步历史（追加，保留5条）
```

**价值**：可以回溯"上周我们在做什么"。

#### 创新5：结构化学术提问

```
四部分模板：
Part 1: Context（研究上下文）
Part 2: Why I'm Asking（问题动机）
Part 3: My Question（具体问题）
Part 4: Output Requirements（颗粒度要求）

+
/analyze-answer 文献分类：
🔴 核心（必须下载）
🟡 参考（记录引用即可）
⚪ 存疑（待评估）
```

**价值**：学术AI（如Consensus）默认给结论，这个模板强制要方法论/样本/效应量。

#### 创新6：PDF处理流水线

```
工具链：
PDF文件 → convert_api.py (Datalab API) → Markdown 
        → process.py (重命名为论文标题)
        → 自动更新 _INDEX.md
        → sync_citations.py (检测Draft中缺失引用)
```

**价值**：论文文献管理完全自动化。

---

## 第三部分：转型规划 — 从"论文项目"到"通用研究工具"

### 3.1 通用化策略

**核心思路**：保留机制，抽象领域

| 当前实现 | 通用化后 | 改动点 |
|----------|---------|--------|
| `thesis-project.md` (论文规则) | `research-project.md` (研究规则模板) | 移除论文特定内容(P1-P4) |
| `Reference/` (100+篇论文) | `Knowledge/` (任何知识库) | 改名+抽象 |
| `Target/Draft.md` (论文) | `Output/main.md` (任何产出物) | 改名 |
| APA/GB引用格式 | 可配置的引用格式 | 参数化 |
| 变量P1-P4 | 用户自定义研究框架 | 模板化 |

### 3.2 OpenAgents 架构设计

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     Academic Research Network (OpenAgents版)                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Agent 层 (6个Agent)                           │   │
│  │                                                                       │   │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐     │   │
│  │  │ Facilitator│  │ Literature │  │  Critical  │  │   Method   │     │   │
│  │  │ (协调者)   │  │   Agent    │  │  Thinker   │  │   Expert   │     │   │
│  │  │            │  │ (文献专家) │  │ (批判者)   │  │ (方法顾问) │     │   │
│  │  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘     │   │
│  │        │               │               │               │            │   │
│  │        └───────────────┴───────────────┴───────────────┘            │   │
│  │                                │                                      │   │
│  │                        ┌───────┴───────┐                             │   │
│  │                        │  PR Manager   │                             │   │
│  │                        │ (变更管理者)  │                             │   │
│  │                        └───────────────┘                             │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                     │                                       │
│                                     ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Mod 层 (扩展模块)                             │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │   │
│  │  │ Document Mod │  │ Workflow Mod │  │  Memory Mod  │               │   │
│  │  │ (文件访问)   │  │ (工作流执行) │  │ (记忆管理)   │               │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 Agent 职责映射

| Agent | 来源 | 核心工作流 | 工具需求 |
|-------|------|-----------|---------|
| **Facilitator** | 新增 | /start, /sync | 读取 _STATE.md, _CONTEXT.md |
| **Literature Agent** | thesis-project.md 分离 | /add-paper, /deep-read | PDF转换, 文献库搜索 |
| **Critical Thinker** | thesis-project.md "批判性" | /analyze-answer | 对其他Agent输出的审查 |
| **Method Expert** | consensus.md "颗粒度要求" | /ask-academic-ai | 方法论知识库 |
| **PR Manager** | merge-pr.md | /create-pr, /merge-pr | 文件读写, 引用检查 |

### 3.4 自定义 Mod 设计

#### Document Mod (文件访问)
```python
class DocumentMod:
    """允许Agent访问本地文件系统"""
    
    def read_file(self, path: str) -> str:
        """读取文件内容"""
        
    def write_file(self, path: str, content: str):
        """写入文件（带版本检查）"""
        
    def search_files(self, query: str, directory: str) -> List[str]:
        """搜索文件内容"""
        
    def convert_pdf(self, pdf_path: str) -> str:
        """调用Datalab API转换PDF"""
```

#### Memory Mod (记忆管理)
```python
class MemoryMod:
    """实现 /reflect 工作流逻辑"""
    
    def extract_memory(self, conversation: str) -> Optional[Memory]:
        """从对话中智能提取记忆"""
        
    def should_remember(self, content: str) -> bool:
        """评估是否值得记忆"""
        
    def update_memory(self, memory: Memory):
        """更新 MEMORY.md"""
```

---

## 第四部分：下一步行动计划

### Phase 1: 最小可行验证 (MVP) — 1-2天
目标：让2个Agent能在OpenAgents中协作

- [ ] 配置 `network.yaml` (启用 project mod)
- [ ] 创建 `facilitator.yaml` (Router模式)
- [ ] 创建 `literature_agent.yaml` (YAML版本)
- [ ] 测试：用户发请求 → Facilitator分派 → Literature回复

### Phase 2: 核心工作流迁移 — 2-3天
目标：将关键工作流迁移到Agent

- [ ] 将 `/create-pr` 逻辑写入 PR Manager Agent
- [ ] 将 `/merge-pr` 逻辑写入 PR Manager Agent
- [ ] 将 `/add-paper` 逻辑写入 Literature Agent (Python版)
- [ ] 实现 PDF转换工具调用

### Phase 3: 完整系统 — 3-4天
目标：所有6个Agent协作

- [ ] 添加 Critical Thinker Agent
- [ ] 添加 Method Expert Agent
- [ ] 实现 Memory Mod
- [ ] 实现 Document Mod
- [ ] 端到端测试

### Phase 4: 演示与提交 — 1天
目标：准备Hackathon提交材料

- [ ] 录制3分钟演示视频
- [ ] 整理GitHub仓库
- [ ] 完善README

---

## 第五部分：文件结构重组

### 新项目结构

```
academic-research-network/
├── network.yaml                    # OpenAgents网络配置
├── agents/                         # Agent定义
│   ├── facilitator.yaml
│   ├── literature_agent.py         # Python版本(需要访问文件)
│   ├── critical_thinker.yaml
│   ├── method_expert.yaml
│   └── pr_manager.py              # Python版本(需要读写PR)
├── mods/                          # 自定义Mod
│   ├── document_mod.py
│   └── memory_mod.py
├── tools/                         # 工具集
│   ├── pdf_converter.py           # 从convert_api.py提取
│   └── citation_checker.py        # 从sync_citations.py提取
├── templates/                     # 研究项目模板
│   ├── research_project/          # 通用研究项目
│   │   ├── Knowledge/
│   │   ├── Discussions/
│   │   ├── Changes/
│   │   ├── Output/
│   │   ├── MEMORY.md
│   │   └── _STATE.md
│   └── thesis_project/            # 论文特化模板
├── workflows/                     # 工作流定义(供Mod读取)
│   ├── start.md
│   ├── create-change.md
│   ├── merge-change.md
│   └── ...
├── README.md
├── start_network.ps1              # 一键启动
└── demo/                          # 演示材料
    └── video/
```

---

## 附录：竞争优势总结

### vs 其他Hackathon项目

| 我们的优势 | 说明 |
|-----------|------|
| **真实验证** | 2个月+实际使用，不是演示项目 |
| **完整工作流** | 9个成熟工作流，不是临时拼凑 |
| **负面约束** | 独创设计理念，解决AI幻觉 |
| **PR驱动** | 类Git的变更管理，可追溯 |
| **工具链完整** | PDF转换、引用检查、状态同步 |

### vs OpenAgents官方Demo

| 官方Demo | 我们的项目 |
|---------|-----------|
| 通用Research Team | **领域特化**（学术研究） |
| 简单任务委派 | **完整工作流**（多步骤验证） |
| 无持久化 | **记忆+状态系统** |
| 无负面约束 | **禁止列表优先** |
